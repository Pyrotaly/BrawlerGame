using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Char0AirLightChargedAttackState : AirLightChargedAttackState
{
    public Char0AirLightChargedAttackState(Player player, string animBoolName) : base(player, animBoolName)
    {
    }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
